'use strict';

function refresh() {
    generateQuestionsElement();
    //handleAnswerClick();
    // handleAnswerCheck();
    // handleToNextPage();
  }
  
  // when the page loads, call `refresh`
  $(refresh);
