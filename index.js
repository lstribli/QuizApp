'use strict';

function refresh() {
    startQuiz();
    formEventHandler();
    //handleAnswerClick();
    // handleAnswerCheck();
    // handleToNextPage();
  }
  
  // when the page loads, call `refresh`
  $(refresh);
